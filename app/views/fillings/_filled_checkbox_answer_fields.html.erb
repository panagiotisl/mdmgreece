<tr>
  <td>
    <div class="checkbox">
      <label>
        <input type="checkbox" name="<%= question_id %>[]" value="<%= choice_id %>" <%= @show ? 'disabled="true"' : '' %> <%= !checked ? "checked" : "" %> > <%= content %>
      </label>
    </div>
  </td>
</tr>
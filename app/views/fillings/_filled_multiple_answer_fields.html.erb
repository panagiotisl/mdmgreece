<tr>
    <td>
      <div class="radio">
        <label>
          <input type="radio" name="<%= question_id %>" value="<%= choice_id %>" <%= @show ? 'disabled="true"' : '' %> <%= !checked ? "checked" : "" %> > <%= content %>
        </label>
      </div>
    </td>
</tr>

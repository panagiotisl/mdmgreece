<tr>
    <td>
      <div class="radio">
        <label>
          <input type="radio" name="<%= question_id %>" value="<%= choice_id %>" <%= choice_id.to_s == params[question_id.to_s] ? "checked" : "" %> > <%= content %>
        </label>
      </div>
    </td>
</tr>
